import React, { useState } from 'react';
import LeafletMap from '../components/LeafletMap';

const LocationQueryPage = () => {
  return (
    <div>
      <h2>Location Query</h2>
      <LeafletMap />
    </div>
  );
};

export default LocationQueryPage;
